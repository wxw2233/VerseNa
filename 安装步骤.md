# VerseNa WebUI 安装步骤

本文只部署 VerseNa WebUI，不安装、不启动 Electron。

## 一、Windows

### 1. 安装基础依赖

使用管理员 PowerShell 执行：

~~~powershell
winget install --id Git.Git -e
winget install --id OpenJS.NodeJS.LTS -e
winget install --id Python.Python.3.11 -e
~~~

安装完成后关闭 PowerShell，重新打开一个窗口，确认命令可用：

~~~powershell
git --version
node --version
npm.cmd --version
py -3.11 --version
~~~

### 2. 获取源码

~~~powershell
$InstallRoot = "D:\VerseNa"
New-Item -ItemType Directory -Force -Path $InstallRoot | Out-Null
Set-Location $InstallRoot
git clone https://github.com/wxw2233/VerseNa.git
cd VerseNa
~~~

如果目录已经是通过 Git 克隆的源码，使用：

~~~powershell
cd "D:\VerseNa\VerseNa"
git pull --ff-only
~~~

### 3. 创建 Python 虚拟环境

~~~powershell
py -3.11 -m venv backend\.venv
.\backend\.venv\Scripts\python.exe -m pip install --upgrade pip
.\backend\.venv\Scripts\python.exe -m pip install -r .\backend\requirements-runtime.txt
~~~

如果系统没有 \`py -3.11\`，但 \`python --version\` 显示 Python 3.10 或更高版本，可以使用：

~~~powershell
python -m venv backend\.venv
.\backend\.venv\Scripts\python.exe -m pip install --upgrade pip
.\backend\.venv\Scripts\python.exe -m pip install -r .\backend\requirements-runtime.txt
~~~

### 4. 安装前端依赖并构建 WebUI

~~~powershell
cd frontend
npm.cmd ci
npm.cmd run build
cd ..
~~~

不要执行 \`npm install\` Electron 目录，也不要执行 \`scripts/start-electron.ps1\`。

### 5. 启动 WebUI

为了确保启动脚本使用刚才创建的虚拟环境：

~~~powershell
$env:Path = "$PWD\backend\.venv\Scripts;$env:Path"
powershell -ExecutionPolicy Bypass -File .\scripts\start-lan.ps1
~~~

启动成功后，终端会打印访问地址，例如：

~~~text
http://192.168.1.20:8002
~~~

本机访问也可以使用：

~~~text
http://127.0.0.1:8002
~~~

### 6. Windows 防火墙

如果需要让同一局域网的其他设备访问，使用管理员 PowerShell 执行一次：

~~~powershell
New-NetFirewallRule -DisplayName "VerseNa WebUI 8002" -Direction Inbound -Protocol TCP -LocalPort 8002 -Action Allow
~~~

### 7. 获取访问令牌

首次以局域网模式启动时，令牌会打印在启动终端中。默认也会保存到：

~~~powershell
Get-Content .\backend\data\access_token
~~~

如果项目通过 \`VERSENA_DATA_DIR\` 或 \`VERSENA_ACCESS_TOKEN_FILE\` 自定义了数据目录或令牌文件位置，请从部署时保存的配置记录中确认路径；不要在截图、日志或仓库中公开 \`backend\.env\` 的内容。

登录后可在 **设置 -> 访问安全** 修改令牌，最少 6 个字符。

## 二、Termux

### 1. 安装基础依赖

~~~bash
pkg update
pkg upgrade -y
pkg install -y git
termux-setup-storage
~~~

在弹出的权限请求中允许 Termux 访问存储。

### 2. 获取源码

~~~bash
cd ~
git clone https://github.com/wxw2233/VerseNa.git
cd ~/VerseNa
~~~

如果目录已经是 Git 源码：

~~~bash
cd ~/VerseNa
git pull --ff-only
~~~

### 3. 一键安装依赖并构建 WebUI

~~~bash
bash scripts/setup-termux.sh
~~~

该脚本会自动完成：

- 安装 Termux 的 Python、Git 和 Node.js
- 创建 \`VerseNa/.venv\`
- 安装 Termux 专用 Python 依赖
- 安装前端 npm 依赖
- 构建 \`frontend/dist\`
- 验证后端能正常导入

Termux 使用 \`backend/requirements-termux.txt\`，其中固定了兼容 Termux 的 Pydantic 版本，不要改用 Windows 的运行时依赖文件。

### 4. 启动 WebUI

~~~bash
bash scripts/start-termux.sh
~~~

启动后终端会显示端口和数据目录。浏览器访问：

~~~text
http://127.0.0.1:8002
~~~

局域网其他设备访问 Termux 所在设备的局域网 IP：

~~~text
http://手机局域网IP:8002
~~~

### 5. 获取访问令牌

首次启动时令牌会打印在 Termux 终端。默认令牌文件为：

~~~bash
cat "$HOME/.local/share/versena/access_token"
~~~

Termux 的数据库、令牌、技能数据和工具工作目录默认保存在：

~~~text
$HOME/.local/share/versena
~~~

不要把运行数据放在 \`/sdcard\` 或 \`~/storage\` 下，否则 SQLite 文件锁和写入可能异常。

## 三、启动失败处理

### Python 依赖缺失

Windows：

~~~powershell
.\backend\.venv\Scripts\python.exe -m pip install -r .\backend\requirements-runtime.txt
~~~

Termux：

~~~bash
bash scripts/setup-termux.sh
~~~

### \`npm\` 被 PowerShell 禁止执行

Windows 下使用 \`npm.cmd\`：

~~~powershell
npm.cmd ci
npm.cmd run build
~~~

### \`frontend/dist/index.html\` 不存在

重新构建前端：

~~~powershell
cd frontend
npm.cmd ci
npm.cmd run build
cd ..
~~~

Termux：

~~~bash
bash scripts/setup-termux.sh
~~~

### 端口被占用

检查 8002 端口：

~~~powershell
Get-NetTCPConnection -LocalPort 8002 -ErrorAction SilentlyContinue
~~~

~~~bash
pkg install -y net-tools
netstat -tln | grep 8002
~~~

停止旧 VerseNa 进程后再启动。也可以在 \`backend/.env\` 和启动环境中将端口改成其他未占用端口。

## 四、源码更新

首次手动安装完成后，后续可以在 WebUI 的 **设置 -> 源码更新** 中检查和应用更新，更新完成后重新启动 WebUI。

手动更新方式：

Windows：

~~~powershell
cd "D:\VerseNa\VerseNa"
git pull --ff-only
.\backend\.venv\Scripts\python.exe -m pip install -r .\backend\requirements-runtime.txt
cd frontend
npm.cmd ci
npm.cmd run build
cd ..
$env:Path = "$PWD\backend\.venv\Scripts;$env:Path"
powershell -ExecutionPolicy Bypass -File .\scripts\start-lan.ps1
~~~

Termux：

~~~bash
cd ~/VerseNa
git pull --ff-only
bash scripts/setup-termux.sh
bash scripts/start-termux.sh
~~~

不要使用 \`git reset --hard\`，避免删除本地主题、素材或配置。更新前应先处理 Git 未提交的源码改动。

## 五、运行规则

- 纯 WebUI 不需要安装 Electron 依赖。
- 不执行 \`scripts/start-electron.ps1\`。
- 不执行 \`cd electron && npm install\`。
- Windows WebUI 使用 \`backend/requirements-runtime.txt\`。
- Termux WebUI 使用 \`backend/requirements-termux.txt\`。
- 运行中的终端窗口需要保持打开；按 \`Ctrl+C\` 可停止 VerseNa。
